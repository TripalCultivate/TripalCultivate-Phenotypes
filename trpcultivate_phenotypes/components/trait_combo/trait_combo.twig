<div {{ attributes.addClass('tcp-trait-combo') }}>

	<def>
		{{ name }}

    {% if status %}
      {% for status_item, status_value in status %}
        {% if status_value == 1 %}
				  <span {{ attributes.setAttribute('class', 'tcp-pheno-' ~ status_item) }}>
					  {{ include('trpcultivate_phenotypes:icon_text', { icon_name: status_item }) }}
          </span>
        {% endif %}
   		{% endfor %}
    {% endif %}
	</def>

	<em>{{ definition }}</em>

    {{ multiselect_method ? '<div>' : '' }}
		  {% for method in method_unit_combo %}
			  <def>{{ method['method_shortname'] }}: ({{ method['unit'] }})
				  <small>{{ method  ['type'] }}</small>
			  </def>
			  {{ method['collection_method'] }}

				{% if multiselect_method %}
           {{ field_label }} {{ add }}
				{% endif %}

		  {% endfor %}
    {{ multiselect_method ? '</div>' : '' }}

</div>
